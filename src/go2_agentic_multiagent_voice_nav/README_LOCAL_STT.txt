This package switches speech transcription to local-only STT using faster-whisper.

Install dependencies:
  pip3 install faster-whisper pyttsx3 requests pyyaml pillow numpy

If you use Omi:
  pip3 install omi

Use OpenRouter only for:
- conversation fallback
- camera/VLM description

No speech API call is used.
